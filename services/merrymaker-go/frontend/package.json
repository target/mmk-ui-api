{
  "name": "merrymaker",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "build:dev": "DEV=true bun run build.ts",
    "build:prod": "NODE_ENV=production bun run build.ts",
    "dev": "DEV=true bun run build.ts",
    "watch": "DEV=true bun run --watch build.ts",
    "test": "bun test --preload ./test-setup.js",
    "lint": "biome lint",
    "lint:fix": "biome lint --write",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/bun": "latest",
    "happy-dom": "^20.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@jmespath-community/jmespath": "1.3.0",
    "htmx.org": "2.0.8",
    "lucide": "0.555.0"
  }
}
